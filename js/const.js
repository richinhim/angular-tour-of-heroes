function getCircleVolume(radius){
  const PI = 3.14; // const 로 고쳤다.
  return radius * radius * PI;
}

//let circleRadius = 5;
//let volume = getCircleVolume(circleRadius);
circleRadiud = 5;
var volume = getCircleVolume(circleRadiud);
console.log(volume);
